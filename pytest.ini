[pytest]
pythonpath = backend agent
testpaths = tests
markers =
    slow: trains YOLO (downloads yolov8n weights); run with RUN_SLOW=1
    live: needs the agent container / harness with Ollama on AGENT_URL
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
